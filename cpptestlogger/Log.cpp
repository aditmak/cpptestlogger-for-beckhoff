#include "pch.h"
#include "Log.h"

/*AmsAddr Addr; //Address of AMS
PAmsAddr pAddr = &Addr; //Pointer of Address of AMS

void Adsconnect(unsigned short address) {
	long errorcode, nPort;

	nPort = AdsPortOpen();
	errorcode = AdsGetLocalAddress(pAddr);

	if (errorcode)
		spdlog::error("AdsGetLocalAddress failed with Error Code {}", errorcode);
	else
		spdlog::info("ADS Port Open for connection: {}", address);
	pAddr->port = address;
}

//ADS file logging

void Adsfilelog();

// ADS console logging

void Adsconsolelog();*/
